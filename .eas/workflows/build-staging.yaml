name: Staging build

on:
  push:
    branches: ["dev"]

jobs:
  build_android:
    type: build
    params:
      platform: android
      profile: staging

  build_ios:
    type: build
    params:
      platform: ios
      profile: staging
